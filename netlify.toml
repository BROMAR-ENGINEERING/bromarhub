[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "."

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_URL"

[[redirects]]
  from = "/api/submit-timesheet"
  to = "/.netlify/functions/submit-timesheet"
  status = 200

[functions]
  node_bundler = "esbuild"
